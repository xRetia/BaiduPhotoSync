# 超限视频压缩上传与文件对比模式

**作者：Manus AI**  
**适用范围：** Windows 版一刻同步的高级同步设置。

## 功能概览

当普通账号的视频文件超过服务端 30 MB 限制时，用户可在“高级设置 → 视频”启用 **压缩视频到30M以内**。程序会保持本地高清原件完全不变，在临时目录创建一个压缩副本，使用原始文件名上传该副本，待文件上传工作进程结束后清理临时目录。这样，本地仍保留高清版本，目标相册中保存可上传的压缩版本。

> 压缩功能只处理支持的视频扩展名；照片和其他文件绝不会被转码。若未勾选此项，原有“跳过超过普通用户大小限制的文件”规则仍然生效。

| 项目 | 实现规则 |
|---|---|
| 上传目标 | 28 MiB，而非名义 30 MiB；为容器与码率控制波动预留余量 |
| 本地文件 | 不修改、不移动、不删除高清原件 |
| 云端文件名 | 使用本地原文件名上传压缩副本 |
| 临时文件 | 仅位于系统临时目录，工作进程结束后清除 |
| 适用格式 | MP4、MOV、M4V、AVI、MKV、WMV、FLV、WebM、3GP、3G2、MPG、MPEG、TS、MTS、M2TS |
| 上传并发 | 大于等于 16 MiB 的传输自动独占上行链路，防止多路大视频相互造成写超时 |

## 编码器探测与回退

程序不会只根据“FFmpeg 编译时包含某编码器”判断硬件可用，而是在启动首个压缩任务时使用极小测试帧验证当前机器的驱动、设备和硬件会话。这样能避免装有 NVENC/QSV/AMF 支持的 FFmpeg 在实际机器没有对应显卡或驱动时，每个视频都先失败一次。

硬件编码优先级是 **AMD AMF → NVIDIA NVENC → Intel QSV → libx264**。对 AMD，编码器为 `h264_amf`。AMD 官方资料将 H.264 AMF 的编码器名列为 `h264_amf`，并支持 `quality`、`rc`、`b`、`maxrate`、`bufsize` 和 `vbaq` 等参数；实现使用受限码率和质量优先设置。[1] 其他厂商分别使用 `h264_nvenc` 与 `h264_qsv`，仅在真实探测成功时选用。

| 条件 | 操作 |
|---|---|
| 已探测到 AMD 硬件编码且视频码率预算不低 | 优先 `h264_amf`，H.264、质量优先、峰值码率受控 |
| AMD 不存在或启动失败 | 尝试可用的 NVIDIA NVENC 或 Intel QSV |
| 硬件编码均不可用 | 自动回退 `libx264`，保证功能仍可完成 |
| AMD 的可用码率过低 | 不以极低码率强行生成模糊高清分辨率；优先降低分辨率，必要时由 `libx264` 处理 |
| 没有任何分辨率能满足最低质量码率 | 明确报错，不上传劣质或超过 28 MiB 的文件 |

## 28 MiB 码率与分辨率策略

程序先用 `ffprobe` 获得时长、宽高和音轨情况，然后按下式为视频流计算预算：

```text
视频码率 = (28 MiB × 8 × 0.94 / 时长) − 音频码率
```

其中有音轨时预留 96 kb/s AAC。实际编码使用相同目标码率、最大码率和两倍缓冲区，以降低输出突破体积上限的概率。若首次输出仍高于 28 MiB，则根据实际输出体积计算更低的重试码率。

分辨率始终按以下次序尝试：**原始分辨率（最多 1080p） → 720p → 480p**。程序仅在当前分辨率的码率低于最低可接受值时才进入下一层，而不是优先缩小画面。最低视频码率阈值为 1080p 1.8 Mb/s、720p 1.05 Mb/s、480p 600 kb/s；这意味着超长视频在 28 MiB 内无法达到最低质量时会被明确报告为不可行，而不会静默压成极低质量文件。

> AMD/FFmpeg 官方资料说明 AMF 支持 H.264 硬件编码及 `-b`、`-maxrate`、`-bufsize` 等速率控制参数；FFmpeg 官方下载页列出 Windows 预编译版本提供者。[1] [2]

## 文件对比模式

高级设置提供以下三项比较方式。默认项为 **智能（推荐）**，兼顾普通备份、服务端内容去重和“本地高清 + 云端压缩同名视频”的流程。

| 模式 | 同名同内容 | 同名视频但云端为压缩版 | 异名同内容 | 同名非视频但内容不同 |
|---|---|---|---|---|
| **智能（默认）** | 已同步 | 已同步 | 已同步 | 已同步（沿用保守同名策略） |
| 仅按文件名 | 已同步 | 已同步 | 视为新增并上传 | 已同步 |
| 内容优先 | 已同步 | 已同步 | 已同步 | 标记为冲突，等待人工确认 |

无论选择哪种模式，**同名的视频压缩副本始终视为已同步**。这是为了确保重新扫描时，本地高清版本不会因为大小/MD5 与云端压缩版本不同而被重复上传。

## 按需下载 FFmpeg 与 Windows 打包

默认发布包**不附带 FFmpeg**。用户勾选“压缩视频到30M以内”时，程序先检测应用目录、当前用户下载目录和系统 PATH；若没有可用 FFmpeg，才显示下载弹窗，并从 [BtbN FFmpeg-Builds 最新发布页](https://github.com/BtbN/FFmpeg-Builds/releases)下载 Windows x64 GPL 静态构建。弹窗会显示已下载大小、总大小、百分比和实时网速。

下载器先获取发布页的 `checksums.sha256`，再下载 `ffmpeg-master-latest-win64-gpl.zip` 并计算 SHA-256；只有两者完全一致时才解压 `ffmpeg.exe` 和 `ffprobe.exe` 到当前 Windows 用户的应用数据目录。下载、校验或解压失败时会删除不完整文件、关闭视频压缩开关，不会执行未校验的二进制。所用构建包含 AMF、NVENC 与 QSV 等硬件编码支持；其 GPL 许可证仍适用于该可选组件。[3]

## 回归验证

本地回归测试已覆盖以下结果：

| 测试 | 结果 |
|---|---|
| 超过 30 MiB 的 TS 视频，启用压缩 | 生成“压缩后上传”计划项 |
| 超过 30 MiB 的 TS 视频，关闭压缩 | 生成“跳过”计划项 |
| 同名但大小/MD5 不同的视频 | 三种对比模式均不重复上传 |
| 内容优先模式的同名不同图片 | 标记为冲突 |
| 1080p 视频分辨率阶梯 | 依次为 1080p、720p、480p |
| 无可用 GPU 的本地环境 | 经过实际设备探测后回退 `libx264` |
| 临时转码 | 成功在预算内输出，源文件 SHA-256 不变 |

## 参考

[1]: [AMD AMF Encoder Settings and Tuning in FFmpeg](https://github.com/GPUOpen-LibrariesAndSDKs/AMF/wiki/AMF-Encoder-Settings-and-Tuning-in-FFmpeg)
[2]: [FFmpeg Hardware AMF documentation](https://trac.ffmpeg.org/wiki/Hardware/AMF)
[3]: [BtbN FFmpeg-Builds Releases](https://github.com/BtbN/FFmpeg-Builds/releases)
